[package]
name = "rwa-oracle"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
odra = "2.7.2"

[dev-dependencies]
odra-test = "2.7.2"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(odra_module, values("RwaOracle"))'] }

